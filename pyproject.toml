[project]
name = "scn1a"
version = "0.1.0"
description = "SCN1A variant pathogenicity prediction with ESM-2 embeddings."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["scn1a"]
